name: Run Unit Tests and Lint Files
on:
  workflow_call:
  pull_request:

jobs:
  code-formatter-linter-tests:
    name: Run Formatter, Linter and Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Run formatter, linter and tests
        run: |
          bash run-test.sh